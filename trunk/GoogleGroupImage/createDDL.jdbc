CREATE TABLE DOUBLEVALUE (ID INTEGER NOT NULL, FEATURE_VALUE FLOAT, IMAGEFEATURE_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE IMAGEFEATURE (ID INTEGER NOT NULL, RELATIVE_PATH VARCHAR(255), FEATURE_ID VARCHAR(255), PRIMARY KEY (ID))
ALTER TABLE DOUBLEVALUE ADD CONSTRAINT DBLVALUEMGFATUREID FOREIGN KEY (IMAGEFEATURE_ID) REFERENCES IMAGEFEATURE (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL, PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
